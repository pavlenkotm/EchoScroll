{
  "name": "wasm-contract",
  "version": "1.0.0",
  "description": "AssemblyScript WASM contract for NEAR",
  "scripts": {
    "build": "asb",
    "test": "asp",
    "deploy": "near deploy --wasmFile build/release.wasm"
  },
  "keywords": ["near", "assemblyscript", "wasm", "smart-contract"],
  "devDependencies": {
    "near-sdk-as": "^3.2.3",
    "assemblyscript": "^0.27.0"
  }
}
